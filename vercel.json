{
    "version": 2,
    "builds": [
        {
            "src": "src/bot.js",
            "use": "@vercel/node",
            "config": {
                "buildCommand": "npm install"
            }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "src/bot.js"
        }
    ]
}